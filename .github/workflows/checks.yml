name: checks
# Contribution gate. The Python-era steps (ast.parse of amux-server.py, inline
# <script> extraction, check_client_refs, pytest) were removed with the Python
# server on 2026-08-09 — the Rust build/lint/test gates live in rust.yml.
# What remains is the repo-wide SECRET SCAN, which is language-agnostic and
# must run on every push: collaborators push straight to main (no local hook
# guaranteed), and AC-239 was four credentials committed in a PUBLIC repo that
# only a CI-side scan would have caught.
on:
  push:
    branches: [main]
  pull_request:

jobs:
  checks:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # Full history: the client-version guard below diffs against the push's
      # base, which a shallow clone cannot reach.
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Browser evidence and lifecycle cleanup controls (AF-768, AF-773)
        run: |
          npm ci --ignore-scripts
          node --test tests/ci-e2e-evidence.test.mjs tests/worker-lifecycle-cleanup.test.mjs
      # AF-148 / AMUX-3524 follow-up: a client JS change that ships WITHOUT a
      # version bump reaches no browser holding the cached script, and nothing
      # said so. dashboard_assets.rs pins that APP_VER and CACHE AGREE — but
      # 25ba8ea changed app.js and bumped NEITHER, so they still agreed and the
      # test stayed green. The silent shape is worse than a mismatch: the
      # author's find-and-replace bump no-opped (a peer had already moved the
      # version), the functional edits in the same pass succeeded, and the
      # script printed success. Diffing is the only way to see it.
      - name: Client JS changes carry a version bump (CLAUDE.md, enforced)
        run: |
          set -uo pipefail
          BASE="${{ github.event.pull_request.base.sha || github.event.before }}"
          if [ -z "$BASE" ] || [ "$BASE" = "0000000000000000000000000000000000000000" ] \
             || ! git cat-file -e "$BASE^{commit}" 2>/dev/null; then
            echo "no reachable base ($BASE) — first push or force-push; guard SKIPPED, honestly"
            exit 0
          fi
          CHANGED=$(git diff --name-only "$BASE" HEAD -- \
            crates/amux-dashboard/static/app.js crates/amux-dashboard/static/sw.js)
          [ -n "$CHANGED" ] || { echo "no client JS in this range — nothing to check"; exit 0; }
          echo "client JS changed:"; echo "$CHANGED" | sed 's/^/  /'
          # The VERSION LINES specifically, not the files: a file can change
          # while its version does not, which is exactly the defect.
          APP_MOVED=$(git diff "$BASE" HEAD -- crates/amux-dashboard/static/app.js \
            | grep -c '^[+-].*APP_VER *=' || true)
          SW_MOVED=$(git diff "$BASE" HEAD -- crates/amux-dashboard/static/sw.js \
            | grep -c '^[+-].*CACHE *=' || true)
          if [ "$APP_MOVED" -eq 0 ] || [ "$SW_MOVED" -eq 0 ]; then
            echo
            echo "FAIL: client JS shipped without bumping both versions."
            echo "  APP_VER line changed: $([ "$APP_MOVED" -gt 0 ] && echo yes || echo NO)"
            echo "  sw.js CACHE changed:  $([ "$SW_MOVED" -gt 0 ] && echo yes || echo NO)"
            echo "A browser holding the cached script never fetches this change."
            echo "Bump BOTH (read-then-increment; a literal find-and-replace can"
            echo "silently no-op when a peer moved the version first - AF-148)."
            exit 1
          fi
          echo "both version lines moved — cached clients will refetch"
      - name: Secret scan (the pre-commit hook, in CI)
        run: |
          set -euo pipefail
          PATTERNS=(
            "sk-ant-api[0-9a-zA-Z-]{20,}" "AKIA[0-9A-Z]{16}" "ghp_[A-Za-z0-9]{36}"
            "sk_(test|live)_[A-Za-z0-9]{20,}"
            "(R2|AWS)_SECRET(_ACCESS)?_KEY[[:space:]]*=[[:space:]]*[A-Za-z0-9/+]{32,}"
            "CLERK_SECRET_KEY[[:space:]]*=[[:space:]]*[A-Za-z0-9_-]{20,}"
            "xox[baprs]-[A-Za-z0-9-]{10,}" "glpat-[A-Za-z0-9_-]{20,}"
          )
          # Self-test FIRST: a planted secret must be caught, or a green run
          # here means nothing (the scanner that missed AC-239 was also green).
          # Built at RUNTIME, never written literally in this file — a literal
          # here would be found by the scan below and fail the repo on its own
          # self-test, forever. (Caught before shipping: the first cut did
          # exactly that.)
          printf 'CLERK_SECRET_KEY=sk_%s_%s\n' "test" "$(printf 'A%.0s' $(seq 28))" > /tmp/_probe
          grep -Eq -e "sk_(test|live)_[A-Za-z0-9]{20,}" /tmp/_probe \
            || { echo "SELF-TEST FAILED: scanner cannot catch a planted secret"; exit 1; }
          rm -f /tmp/_probe
          fail=0
          for p in "${PATTERNS[@]}"; do
            if out=$(grep -REn -e "$p" --include='*.py' --include='*.sh' --include='*.yml' \
                      --include='*.yaml' --include='*.json' --include='*.ts' --include='*.js' \
                      --include='*.rs' --include='*.toml' . 2>/dev/null); then
              echo "SECRET PATTERN MATCHED: $p"; echo "$out" | head -3 | cut -c1-120; fail=1
            fi
          done
          [ "$fail" -eq 0 ] && echo "secret scan clean" || exit 1

      # frustrations.md structural integrity (AF-28). NOT a reintroduction of the
      # Python-era pytest gate removed above — this validates a committed MARKDOWN
      # file's contract, is language-agnostic like the secret scan, and exists
      # because the check that used to run it was deleted with tests/ on the same
      # day the utility it runs was deliberately KEPT. A script nobody runs is
      # decoration (ethos rule 1), and this one already caught a real defect on its
      # first run: an entry using `FIX-NOTE:` where the contract says `FIX:`, which
      # `grep '^FIX:'` silently skipped.
      #
      # Exit codes are load-bearing: 0 clean, 1 real problems, 2 board unreachable
      # AND nothing structural wrong. 2 is EXPECTED in CI (no localhost board) and
      # must not fail the build. Only 1 fails, so a green run here means "every entry
      # has its required fields", never "the board agreed", which CI cannot check and
      # should not claim.
      #
      # AEAB-19: the sentence above USED TO BE TRUE ONLY IN INTENT. The script's
      # unreachable branch returned a bare 2, discarding whether it had found
      # problems — and it always takes that branch in CI — so this step could not
      # fail, and the older wording of this comment ("the structural half still ran
      # and is what we are gating on") asserted a gate that did not exist. It ran; it
      # was not gated on. A frustrations entry with no `## ` heading reached main
      # green through this step on 2026-08-17. The script now returns 1 for a
      # structural problem regardless of board reachability, which is what makes the
      # claim above real; scripts/test-frustrations-audit.sh pins it.
      # The `|| rc=$?` is LOAD-BEARING, not style. GitHub runs this with
      # `shell: bash -e {0}`, and `set -uo pipefail` does NOT clear `-e` — so
      # the previous form (`python3 …` on its own line, then `rc=$?`) aborted
      # the whole step the instant the script exited non-zero, and `rc` was
      # never read. Every exit code became a failure, including the 2 that the
      # comment above explicitly says must not fail the build. That made
      # `checks` — the ONLY required status check on main — red on every push
      # for reasons unrelated to what it gates, which is how a required check
      # becomes something people bypass by reflex. A command inside a `||` list
      # is exempt from `-e`, so this reads the code instead of dying on it.
      - name: frustrations.md structure (AF-28)
        run: |
          set -uo pipefail
          rc=0
          python3 scripts/frustrations_audit.py || rc=$?
          echo "frustrations_audit exit=$rc (0 clean, 1 real problems, 2 board unreachable)"
          if [ "$rc" = "1" ]; then exit 1; fi
          exit 0

      # AEAB-19. This step exists because the step above could not fail, and a gate
      # nobody can trip is worse than none — it is trusted. Same self-test logic as
      # the secret scan at the top of this job: prove the instrument catches a planted
      # defect before believing a clean run. Every case runs with the board pointed at
      # a closed port, which is this job's real condition and the condition under
      # which the gate was broken; a version run against a live board would pass
      # against the old code and prove nothing.
      #
      # Runs under its own shebang and `set -uo pipefail`, so the `bash -e {0}`
      # wrapper does not leak in and no `|| rc=$?` dance is needed.
      - name: frustrations audit can actually fail (AEAB-19)
        run: |
          set -uo pipefail
          ./scripts/test-frustrations-audit.sh
      # The RETIREMENT tool (AF-243). frustrations-archive.py is the only sanctioned
      # way an entry leaves the ledger, it DELETES from the source file, and it shipped
      # with no coverage at all — then gained the whole `--superseded` disposition on
      # 2026-08-26, still with none. The cells pin the survivors by name on both sides
      # of the target, because "the entry reached the archive" and "the entry left the
      # ledger" are both satisfied by a tool that also ate its neighbour.
      - name: frustrations retirement removes exactly one entry (AF-243)
        run: |
          set -uo pipefail
          ./scripts/test-frustrations-archive.sh
      # The frustration SCANNER's own cells (AF-255). The harness existed and was
      # never wired here, so the classifier the sweep depends on had 6 tests that
      # nobody ran. Its false positives are expensive in a specific way: a wrong
      # candidate spends a session's judgment, and a wrong MARKER attributes a
      # mood to Ethan from words he did not write.
      - name: frustration scanner classifies Ethan's words, not what he pasted (AF-255)
        run: |
          set -uo pipefail
          ./scripts/test-frustration-scan.sh
      - name: direct delivery verdicts survive scanner SQL and output (AF-889)
        run: python3 scripts/test-frustration-delivery.py
      # The THEME scanner's cells (AF-341). Wired in the same commit that adds the
      # scanner, because the step above records what happens otherwise: that
      # harness existed unwired and its 6 cells ran for nobody.
      #
      # This one reads TWO repos, and its worst failure is silent in a way the
      # single-repo scanner's is not. The Mixpeek ledger is a checkbox format and
      # the amux one is field-per-line, so they need separate parsers; if the
      # Mixpeek parser stops matching, it returns [] and every cross-repo cluster
      # quietly becomes amux-only. The report still prints, still says `measured`,
      # and Mixpeek reads as clean, which is the precise failure the
      # measured/n_considered contract exists to prevent, reappearing one layer
      # up in the tool that measures it. Cells B and C are that gate.
      - name: friction theme scanner reads BOTH repos, or says it could not (AF-341)
        run: |
          set -uo pipefail
          ./scripts/test-friction-themes.sh
      - name: attachment metadata cannot manufacture cross-repo instructions (AF-888)
        run: python3 scripts/test-friction-attachments.py
      - name: peer collaboration cannot inflate board-drive nudge pressure (AF-890)
        run: python3 scripts/test-friction-nudges.py
      # AF-360. The dashboard stamps every outgoing message with the send time
      # and had no guard against text that already carried one, so re-sending
      # produced a nested stamp. Measured live on MSG-35221. The reader strips a
      # SINGLE leading stamp, so the survivor is a stale timestamp that reaches
      # the model looking exactly like a real one. Client-side and node-only, so
      # it belongs here rather than in rust.yml; the cells drive the shipped
      # functions extracted from app.js rather than a copy.
      - name: the send-time stamp is applied exactly once (AF-360)
        run: |
          set -uo pipefail
          ./scripts/test-send-time-stamp.sh
      # AF-561, THE RATCHET. A scripts/test-*.sh that prints a verdict without
      # `set -e` will report PASS after calling a helper that does not exist —
      # bash writes "command not found" to stderr and the suite carries on, and
      # nobody reads stderr when the last line says PASS. That was true of 41 of
      # 43 harnesses this morning (AF-560/AF-562 fixed them). This step exists so
      # harness 45 cannot quietly reintroduce it. It carries its own fixtures,
      # including one with `command_not_found_handle` and no `set -e` — the inert
      # guard that shipped on 2026-09-07 — which it must REJECT.
      - name: every test harness fails a cell that never ran (AF-561)
        run: |
          set -uo pipefail
          ./scripts/test-harness-guard.sh
      # AF-346, ONE LEVEL UP. The ratchet above pins that a harness cannot report
      # PASS for a cell that never ran. It says nothing about a harness that is
      # never RUN, which is the same defect with a longer fuse: measured today,
      # 32 of 80 scripts/test-* files were named by no workflow and no git-hook,
      # and one of them was scripts/test-target-clause.sh — the harness written
      # for AF-346, whose whole incident was a correct guard that did not run.
      # A ratchet against a recorded baseline, so the 32 stay their owners'
      # decision and number 33 cannot arrive quietly.
      - name: Corrupt board acknowledgements retain unknown outcomes
        run: python3 scripts/test-board-ack-unknown.py
      - name: Cargo worktrees reuse builds without stale commit identity
        run: python3 scripts/test-cargo-worktree-provenance.py
      - name: no harness is written and then never invoked (AF-346)
        run: |
          set -uo pipefail
          ./scripts/test-harness-wired.sh
      # AF-791. The shared-target staleness notice in test-contended.sh. The
      # fixture is a dependency-free crate in a private temp target built with
      # --offline, so this costs two tiny compiles and no network. It only
      # samples the build lock, so a runner with no ~/.amux does not wait on it.
      - name: a content edit with a preserved mtime refreshes the shared target (AF-791)
        run: |
          set -euo pipefail
          bash scripts/test-cargo-target-fingerprint.sh
      # AF-346 second pass + AF-336. `--test <name>` skips the LIB ENTIRELY and
      # prints the smallest number of any selector, so it is the likeliest to be
      # read as a clean suite; and the worktree clause listed every dirty file
      # without saying how many were in the crate actually compiled. Stubs cargo,
      # so it costs no build time here.
      - name: the runner names what a selector skipped, and scopes a dirty tree (AF-346/AF-336)
        run: |
          set -uo pipefail
          ./scripts/test-selector-clauses.sh
      # The debris reaper deletes directories without asking, so what keeps it
      # safe is worth pinning: it takes only amux's own prefixes, only past the
      # age floor, never a live session's scratchpad, and nothing at all without
      # --apply. Runs against a mktemp fixture (AMUX_DEBRIS_ROOTS) and a throwaway
      # git repo, so it touches no real temp root and needs no server or cargo.
      # Two of its cells were vacuous when first written and were rewritten until
      # mutating the guard they name reddens them.
      - name: the debris reaper reaps only amux's own, and only when told (AMUX-4357)
        run: |
          set -uo pipefail
          ./scripts/test-reap-amux-debris.sh
      - name: the machine-cleanup tick acts only where it is allowed to (AMUX-4686)
        run: |
          set -uo pipefail
          ./scripts/test-mac-cleanup-tick.sh
      # AF-677. AF-346's ratchet (above) recorded 31 harnesses as unwired rather
      # than wiring them blind, because that decision belongs to each one's
      # owner (ethos rule 8). This is that decision, made for 28 of the 31:
      # each was run standalone, independently verified as CI-safe (no live
      # production server, no real cargo compile), and is wired here. The
      # remaining 3 are NOT wired, each for a checked, specific reason:
      #   test-target-clause.sh    compiles Rust for real; its cheap half
      #                            (test-selector-clauses.sh) is already above.
      #   test-lineage-render.sh   needs a LIVE server with real, aged board
      #                            history (`/api/why/task/<id>`) to prove the
      #                            renderer matches a real payload rather than a
      #                            synthetic fixture — structurally absent on a
      #                            GitHub-hosted runner, by the script's own
      #                            documented design ("skipping is deliberate").
      #   test-unstamped-ledger.sh same shape, against a live `/api/history`.
      # Their baseline entries now carry that reason instead of "owner to
      # confirm or wire".
      - name: alert summary channel counts stay honest (AMUX-owner-alert)
        run: |
          set -uo pipefail
          bash scripts/test-alert-summary.sh
      - name: an in-place edit of an append-only file is caught
        run: |
          set -uo pipefail
          bash scripts/test-append-only-inplace-edit.sh
      - name: a deleted-then-readded line in an append-only file is caught
        run: |
          set -uo pipefail
          bash scripts/test-append-only-resurrection.sh
      - name: the append-only guard scopes by substring correctly
        run: |
          set -uo pipefail
          bash scripts/test-append-only-substring-scope.sh
      - name: a short sha in --evidence expands to the real artifact
        run: |
          set -uo pipefail
          bash scripts/test-artifact-sha-expand.sh
      - name: the CLI dispatch path preserves quoting through env/exec
        run: |
          set -uo pipefail
          bash scripts/test-cli-quoting.sh
      - name: the commit-intent manifest round-trips
        run: |
          set -uo pipefail
          bash scripts/test-commit-intent.sh
      - name: a cross-group setting composes without a real DOM
        run: |
          set -uo pipefail
          bash scripts/test-cross-group-setting.sh
      - name: an archive move is a MOVE, not a deletion, across both files
        run: |
          set -uo pipefail
          bash scripts/test-frustrations-archive-move.sh
      - name: the installed hook's version marker matches the source
        run: |
          set -uo pipefail
          bash scripts/test-hook-version-marker.sh
      - name: install.sh's hook install references the real source path
        run: |
          set -uo pipefail
          bash scripts/test-install-hook-source-ref.sh
      - name: installer Rust builds exclude uncommitted shared-checkout sources
        run: python3 scripts/test-install-committed-source.py
      - name: install advice does not linger after the install actually lands
        run: |
          set -uo pipefail
          bash scripts/test-install-linger.sh
      - name: a large read is routed to the delegate, an image never is
        run: |
          set -uo pipefail
          bash scripts/test-large-read-delegation.sh
      - name: scripts/mutate.sh applies one string and reverts even if killed
        run: |
          set -uo pipefail
          bash scripts/test-mutate-seams.sh
      - name: a mutation survey names which rule a green suite actually covers
        run: |
          set -uo pipefail
          bash scripts/test-mutate-survey.sh
      - name: mutate.sh's own apply/run/revert contract
        run: |
          set -uo pipefail
          bash scripts/test-mutate.sh
      - name: a sign-in-shaped needsyou ask gets a resume deep link, once
        run: |
          set -uo pipefail
          bash scripts/test-needsyou-signin-link.sh
      - name: board shepherd verb sets/clears the field, and dies on usage without one
        run: |
          set -uo pipefail
          bash scripts/test-board-shepherd.sh
      - name: board archive --archive-outcome sends the field and composes with --authorized-by
        run: |
          set -uo pipefail
          bash scripts/test-board-archive-outcome.sh
      - name: push-consent names who must be asked and who cannot be
        run: |
          set -uo pipefail
          bash scripts/test-push-consent.sh
      - name: a register write merges rather than clobbering a peer's
        run: |
          set -uo pipefail
          bash scripts/test-register-merge.sh
      - name: a second-person resend fires only when it should (positive control)
        run: |
          set -uo pipefail
          bash scripts/test-second-person-resend.sh
      - name: the raw-tmux send fallback stays fenced off the audited path
        run: |
          set -uo pipefail
          bash scripts/test-send-fallback-fence.sh
      - name: a retried send is annotated as a retry, not a fresh message
        run: |
          set -uo pipefail
          bash scripts/test-send-retry-annotation.sh
      - name: CLI retries retain one identity and never inject an unacknowledged fallback
        run: |
          set -uo pipefail
          python3 scripts/test-send-retry-identity.py
      - name: board help never registers an artifact
        run: python3 scripts/test-board-help.py
      - name: Board linked source messages
        run: python3 scripts/test-board-show-messages.py
      - name: the shared-checkout guard attributes a block to the real actor
        run: |
          set -uo pipefail
          bash scripts/test-shared-guard-attribution.sh
      - name: the shipped status hooks survive real durability regressions
        run: |
          set -uo pipefail
          bash scripts/test-status-hooks.sh
      - name: the test receipt describes the bytes it actually compiled
        run: |
          set -uo pipefail
          bash scripts/test-test-receipt.sh
      - name: the tmux backlog probe reports measured/unmeasured honestly
        run: |
          set -uo pipefail
          python3 scripts/test-tmux-backlog.py
      - name: the tmux start guard refuses an ambiguous live owner
        run: |
          set -uo pipefail
          python3 scripts/test-tmux-start-guard.py
      - name: the multiplayer browser contract's shape holds
        run: |
          set -uo pipefail
          node scripts/test-multiplayer-browser-contract.mjs
      # AF-356. The contention wrapper measured ONE of the two ways a peer
      # reddens your suite. Its clean verdict ("NOT build contention") read as
      # "therefore your bug", while a peer's uncommitted source in the shared
      # worktree produces the identical symptom. Cell (c2) is the load-bearing
      # one: the worktree clause must print on the BUILDER arm too, since a
      # caveat inside one branch is one the other branch's reader never sees.
      - name: the contention wrapper reports the worktree, on both arms (AF-356)
        run: |
          set -uo pipefail
          ./scripts/test-contended-worktree.sh
      # AF-371. An mdai run is 82-127s of model calls whose whole product is the
      # output the panel renders. The outbox queued it on a failed fetch and
      # handed the panel a synthetic 202, which is `ok` with no error, so the
      # panel reported a completed run that never left the browser. Reachable on
      # the ordinary path: the auto-builder restarts the server on every commit.
      # Cells 3-5 are the controls that stop an over-broad skip regex from
      # disabling offline sync for the whole app while passing cell 1.
      - name: an mdai run never enters the offline outbox (AF-371)
        run: |
          set -uo pipefail
          ./scripts/test-mdai-outbox.sh
      # AF-342. The staged-guard's SERVER decision has unit tests in
      # git_guard.rs; what a lane actually experiences is what the hook prints,
      # and that half had none. The defect was purely printing volume (93
      # warning lines on a commit with no peer involved), so the behaviour worth
      # pinning is "stays silent when it should" - which no Rust test can see,
      # because the hook is a separate Python file that ships by install, not by
      # build. Language-agnostic, so it belongs here rather than in rust.yml.
      - name: staged-guard prints noise only when it has something to say (AF-342)
        run: |
          set -uo pipefail
          ./scripts/test-staged-guard-render.sh
          # AF-391: the co-edit warning is mtime-derived and says so, and on a
          # shared checkout that makes the busiest lane the default suspect. It
          # now downgrades when the claim is uncorroborated AND nothing is in
          # dispute. Cells (b)(c)(d) are the load-bearing half: real uncommitted
          # content, a two-sided transcript claim, and a check that could not
          # run must all still print in full.
          ./scripts/test-staged-guard-coedit.sh
          # Keep the real hook entry point covered when observations cannot
          # identify a writer, including recorded-owner and blind-peer blocks.
          python3 ./scripts/git-hooks/test_observed_ownership.py
          ./scripts/test-commit-stamp.sh
          ./scripts/test-mass-delete-gate.sh
          ./scripts/test-observed-edits-walk.sh
          ./scripts/test-hook-drift-notice.sh
          # The shared-checkout guard's own 114-case matrix. It existed and ran
          # NOWHERE: not in CI, not in pre-commit. A safety-critical guard whose
          # suite nothing executes is a suite that cannot tell you it broke, and
          # AMUX-3932 was a live bypass in it found by hand rather than by this
          # file. Cheap (no cargo), so there is no reason it was not here.
          python3 ./scripts/git-hooks/test_git_shared_guard.py
      # CLI dispatch tests (AEAB-17). Language-agnostic like the two steps above,
      # and they belong HERE rather than in rust.yml because rust.yml is
      # path-filtered to `crates/**`, `Cargo.*` and `e2e/**` — so a change to
      # `./amux`, which is where both of these bugs lived, triggers NOTHING there.
      # A test that never runs on the file it covers is decoration (ethos rule 1),
      # and `scripts/test-amux-url.sh` had already been sitting unwired since it
      # was written, which is why it is added here too rather than left for later.
      #
      # Both scripts run the SHIPPED dispatch path as a subprocess with HOME and
      # AMUX_API controlled — not a paraphrase of it — and both point AMUX_API at
      # a port nothing listens on, so a case that reaches the network fails loudly
      # instead of mutating a real board. (That isolation is not hypothetical: the
      # first cut of the add-flags test exported AMUX_URL instead of AMUX_API and
      # filed three junk cards on a live board while still reporting green.)
      #
      # These scripts exit non-zero on failure and that is intended to fail the
      # step. Each runs under its own shebang and its own `set -uo pipefail`, so
      # the workflow's `bash -e {0}` wrapper does not leak into them — unlike the
      # frustrations step above, no `|| rc=$?` dance is needed.
      - name: CLI dispatch tests (board add flags, amux url resolver)
        run: |
          set -uo pipefail
          ./scripts/test-board-add-flags.sh
          ./scripts/test-amux-url.sh
          ./scripts/test-discard-folded-into-help.sh
          ./scripts/test-send-fallback-atomic.sh
          ./scripts/test-depends-on-help.sh
      # AMUX-4570: board doing names the right remedy for each refusal. Its own
      # step so a failure is named by step in the job view. (An earlier comment
      # here said the step above reports only its last script. It does not: run
      # blocks execute under bash -e, so that step fails on its first failing
      # script. Corrected under AMUX-4651.)
      - name: CLI refusal remedies (board doing)
        run: ./scripts/test-board-continuation-hint.sh
      # AMUX-4652: board needs reports a refused blocker link instead of claiming it.
      # Its own step for the same reason as the one above.
      - name: CLI board needs checks its link write
        run: ./scripts/test-board-needs-link-refusal.sh
      # AMUX-4654: board needsyou writes the ask, then the status, then the
      # needs:you tag, because the server refuses that tag before the status.
      - name: CLI board needsyou moves before it tags
        run: ./scripts/test-board-needsyou-order.sh
      # AMUX-4653: board request posts request_to, so the card lands on the
      # TARGET's board. Its own step because the server half passes on its own
      # while a CLI still sending `reviewer` leaves the feature dead.
      - name: CLI board request routes to the target
        run: ./scripts/test-board-request-routes-to-target.sh
      # AMUX-4705: a piped brief splits into a one-line title and a body. Its
      # own step because the failure is silent: the card is created, exit 0, and
      # the title is a paragraph.
      - name: CLI piped brief splits into title and body
        run: ./scripts/test-board-piped-title-body.sh
      # AMUX-4685: cdp.mjs tells amux it is driving, over a SELF-SIGNED cert.
      # Its own step because the failure mode is a silent no-op: Node's fetch
      # rejects amux's certificate, the best-effort catch swallows it, and the
      # feature looks exactly like a working one.
      - name: CDP keepalive reaches a self-signed amux
        run: ./scripts/test-cdp-keepalive.sh
      # AMUX-4689: a refused test run must certify nothing. The refusal half
      # compiles nothing; the control reuses the workspace target dir and the
      # smallest crate, so this stays a seconds-scale step in the no-compile job.
      - name: A refused test run certifies nothing
        run: ./scripts/test-contended-refusal.sh
      # AMUX-3891. The CLI is symlinked straight into the working tree and ~50
      # lanes invoke it while it is being edited, so it MUST be parseable in one
      # read: bash holds a byte offset, and a rewrite mid-invocation resumes it
      # in the middle of different bytes. The observed cost was a `board retitle`
      # whose write SUCCEEDED and which then exited 2 with a syntax error, which
      # a caller checking exit codes retries — and retitle is not idempotent for
      # a reader. This asserts the `{ ... }` wrapper and the trailing `exit`+`}`
      # are still the file's shape. A race test cannot hold this line: a green
      # race run is equally consistent with "the shape is gone and we got lucky".
      - name: CLI is offset-safe under in-place rewrite (AMUX-3891)
        run: |
          set -uo pipefail
          ./scripts/test-cli-offset-safe.sh
          ./scripts/test-cli-install.sh
      # AEAB-22. Lives here, not in rust.yml, for the same reason as the other
      # CLI/asset gates: rust.yml is path-filtered to `crates/**` + `Cargo.*` +
      # `e2e/**`, and while app.js IS under crates/, this test also guards
      # scripts/ and needs no Rust toolchain. It loads the SHIPPED functions out
      # of app.js and runs them, so it fails if the linkifier is removed OR if a
      # render path stops calling it — the second being the actual defect it was
      # written for (the previous linkifier was perfect and had zero callers).
      - name: SPA path linkifier (AEAB-22)
        run: |
          set -uo pipefail
          node scripts/test-linkify-paths.mjs

      # AEAB-18. The freshness hook is a SHELL script outside crates/, so
      # rust.yml's path filter never sees it. It also guards a silent,
      # write-shaped failure — appending to a shared append-only file from a
      # diverged checkout succeeds and reaches nobody — which means the only way
      # to know the warning still fires is to fire it. The test builds real git
      # repos and runs the shipped hook against them.
      #
      # Its control cases matter as much as its positive one: a hook that warned
      # unconditionally would pass the incident case while being pure noise, and
      # the hook's own header names silence-when-current as the reason it gets
      # read at all.
      # The disk-clear case is ORDERING, not behaviour: the builder used to delete
      # the cache it was about to fill while a 4x larger idle one sat beside it,
      # so it burned a cold build every 60s and never relieved the pressure.
      # Its control (nothing cleared above the floor) and its last-resort case
      # (the shared dir must STILL be reachable) are what stop the fix becoming
      # either a no-op or a reintroduction of the disk-full outage.
      - name: SessionStart freshness + deployment and watchdog recovery
        run: |
          set -uo pipefail
          ./scripts/test-session-freshness.sh
          ./scripts/test-build-provenance.sh
          ./scripts/test-build-disk-clear.sh
          python3 scripts/test-cargo-target-guard.py
          python3 scripts/test-cargo-budget.py
          ./scripts/test-build-activation-authority.sh
          ./scripts/test-build-activation-launcher.sh
          # A replacement is complete and signed before its one visible rename;
          # persistent listener loss makes the external watchdog reload only the
          # exact configured launch agent.
          ./scripts/test-build-atomic-install.sh
          python3 ./scripts/test-watchdog-recovery.py
          # AEAB-36: a board write that never reached the server must say so,
          # and must never be confused with one the server refused. Its (c)/(d)
          # controls are the load-bearing half — a CLI that shouted "cannot
          # reach the board" at every error would pass the failure cases while
          # destroying the retry/do-not-retry distinction.
          ./scripts/test-board-transport-failure.sh
          # AEAB-48: the push-guard must count what a push ACTUALLY ships. Its
          # case B is the load-bearing one — a guard hollowed out to walk
          # nothing would sail through the rebase case and let genuinely
          # foreign, unpushed work straight past. Both directions are pinned,
          # and both mutations were confirmed to fail before this was wired in.
          ./scripts/test-push-guard-range.sh
          # AF-234: the append-only guard must not accuse a FORK contributor of
          # deleting work they never touched. Cells B and C are the ones that
          # matter — a "fix" that silenced the guard on forks, or reached for a
          # merge-base, would pass the reported case perfectly and stop catching
          # the stale-graft republish the guard exists for. Verified against the
          # pre-fix guard: A and D fail, B and C stay green.
          ./scripts/test-push-guard-fork-base.sh
          # AMUX-3726: the pre-commit gate must answer "is what I am COMMITTING
          # sound", not "is the tree sound". Cell 4 is the load-bearing one —
          # a version that always built the index passes every other cell and
          # taxes the fleet ~22s on every red commit. Verified by mutation, and
          # cell 4's FIRST draft counted worktrees (which the trap cleans up)
          # and missed exactly that mutant; it now asserts on a build marker.
          ./scripts/test-staged-recheck.sh
          # AEAB-52: the e2e harness must SET the seam it depends on. The Rust
          # half is unit-tested; this pins the half that rots silently — a seam
          # nobody sets is decoration (ethos rule 1), and an export placed after
          # serve-head.sh's `exec` would look correct in a diff and never run.
          ./scripts/test-e2e-no-self-adopt.sh
          # AMUX-3965: `start-all` counts spawns that returned, not workers that
          # came up, and fleet-boot.sh independently measures what is actually
          # running. Both numbers were already in the script and nothing compared
          # them, so the boot of 2026-08-30 exited 0 while its own log named six
          # workers it had just reported as started. The cells run the SHIPPED
          # fleet-boot.sh against a stub server; no real worker is started. Cell B
          # is the control that keeps A honest (agreement must stay silent and
          # green), and F separates a genuine `0 started` from a failed parse.
          ./scripts/test-fleet-boot-divergence.sh
          # AF-498: a LaunchAgent loads at GUI LOGIN, not at boot, so an
          # unattended reboot (an OS auto-update overnight) leaves every worker
          # down until a human sits down. fleet-boot's log begins when it RUNS,
          # so that window used to leave no trace anywhere. The three cells drive
          # the arms through AMUX_FLEET_BOOT_EPOCH; cell 2 is the control that
          # keeps the warning from becoming a constant, and cell 3 keeps an
          # unreadable boot time from reading as zero.
          ./scripts/test-fleet-boot-login-gap.sh
          # AMUX-3989: the playwright-mcp wrapper must name a VERSION. `@latest`
          # put an upstream release on the fleet with no amux commit behind it —
          # measured 08-31, the npx cache held 0.0.68 while @latest resolved to
          # 0.0.79. Pure text inspection of the shipped script: no network, no
          # npx, no browser, so it answers the same on a laptop and a runner.
          ./scripts/test-playwright-mcp-pin.sh
          # AMUX-3782: a GRAFTED commit must be as attributable as a committed
          # one. Cell B is the control — every other cell asserts a trailer is
          # present, so without B a version that stamped nothing would pass.
          # Cell D is the one that decides the design: it is green for "call the
          # shipped hook" and red for a hand-rolled interpret-trailers line,
          # because a second producer of the trailer block had already drifted
          # past Amux-Conversation before it was written. All three mutants were
          # confirmed to fail the intended cell and only that cell.
          ./scripts/test-graft-stamp.sh
          # AMUX-3789: the gh shim decides which IDENTITY every fleet gh call
          # uses, so its two failure directions are both expensive. Cell 5 is the
          # control — a shim that always injected would pass every other cell and
          # silently replace a CI token. Cell 4 pins FAIL-OPEN: a broken minter
          # must still run gh, because a shim that can take gh away from the
          # fleet on a bad network is worse than the contention it fixes. Runs
          # against a fake gh and a fake minter, so no cell touches GitHub.
          ./scripts/test-gh-shim.sh
          # AMUX-3797: "which commits were never compiled as committed" must be
          # answerable, and must REFUSE when it cannot be. Cell 3 is the control
          # - with no builder log the naive version reports EVERY commit as
          # unbuilt, which is absence read as emptiness. Cell 5 pins the
          # non-Rust exclusion: the builder targets `git log -1 -- crates/
          # Cargo.toml Cargo.lock`, so counting docs commits as misses read
          # 83-of-235 against a true 0-of-152. Cell 2 pins that a SKIP line is
          # the dedupe, not a missed build.
          ./scripts/test-unbuilt-commits.sh
          # install-hooks.sh must work FROM A LINKED WORKTREE — CLAUDE.md tells
          # every session to develop in one, and `.git` is a FILE there, so the
          # hardcoded "$ROOT/.git/hooks" died mid-run and left the checkout with
          # NO guards. Its case B is the load-bearing half: a test that only
          # asserts "installing works" passes against the pre-fix script from a
          # normal checkout, which is exactly how this went unnoticed. Both
          # directions confirmed to fail before wiring in.
          ./scripts/test-install-hooks-worktree.sh
