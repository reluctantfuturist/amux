# Golden offline queue feedback (AF-775)

The complete ec76baf7 CI run34753024521 finished1011 cases with31 unexpected outcomes. All three golden offline cases stopped at the old3 ops expectation; current product text is Offline — 3 queued, will send on reconnect. The banner intentionally distinguishes queued, failed and awaiting-confirmation states. This correction changes only the test expectation and adds a measured snapshot of the actual banner, queue count and method/path identities to test attachments, CI stdout and the existing client-debug endpoint. Product copy and behavior are unchanged.

The full offline journey still creates three cards through the UI while the browser is offline; checks exactly three board POSTs in durable localStorage; confirms no title on the server before reconnect; waits for the real replay and an empty queue; and checks each title exactly once on the server and in client state. Existing explicitly annotated fetchBoard/online-event fallback behavior is preserved, not promoted to native network proof. The realtime journey remains unchanged: two contexts, exact five-card convergence, a measured dead-SSE gap, watchdog reconnect, exact eight-card convergence and server uniqueness.

AMUX_E2E_WORKING_TREE=1 npx playwright test --config e2e/playwright.config.ts e2e/golden.spec.ts --workers=2 ->6 passed,0failed (3.2m), all desktop/mobile/ios-safari cases, no skips/retries. af775-golden.log and af775-golden-results retain actual queue evidence and client-debug lines for all three projects. Original negative evidence is af773-final-ci-failures.json with full source-linked manifests in af773-final-ci-evidence. No assertion was removed. Clean committed gates, independent review and publication remain outstanding; fullAF768/AF748 are unfinished.
