# iOS session recovery — AF-732

Native audit follow-up: amux restored the saved driver slot without checking whether its WebDriver session still existed. Appium's installed base driver expires a session after 60 seconds without a command by default. Status could report not running while Go kept reusing the dead identifier.

An amux-owned native session now opts out of the idle timeout. Explicit Stop and ownership remain the lifecycle boundary. Individual WebDriver requests still have their existing bounded deadlines. On Go, amux first validates worker/device ownership and reads the saved driver's current URL. Only the explicit WebDriver `invalid session id` response releases that slot and its journal. Unknown/transport failures preserve both. The normal fresh-session path then opens the requested URL; no previous click, typing or command is replayed.

`go_releases_only_a_proven_expired_owned_session_without_replaying_actions` uses a real local HTTP mock and temporary journal. It checks the observed GET count, preserved/deleted file bytes and slot state for live, expired and unknown responses; other workers/devices are refused before any driver request. The focused iOS unit suite passed 8 tests. A recovery emits `expired_session_released` with `measured: true` and `n_considered: 1`; successful Go also reports `recovered_expired_session`.

Native tap calibration is being checked separately against the header audit. API dispatch acknowledgement alone is not a product-state assertion; the audit requires the requested dialog to open and close, and records failed screenshots rather than retrying command taps automatically.

The tap investigation found a different root cause: WebKit was attached to a hidden Safari tab (`document.visibilityState: hidden`, focus false), while XCTest touched the foreground tab. Native screenshot address/title and debugger state referred to different pages. The strict calibration and direct accessibility lookup experiments did not resolve this mismatch and are not shipped. Explicit Go now opens the requested URL through Safari's native deep-link API and chooses a visible Safari debugger context (including redirects). Context enumeration/alignment has a 35-second overall deadline. All native input verbs refuse a hidden debugger tab before dispatch and emit `hidden_native_tab`; successful alignment emits `native_tab_aligned` with measured population. No input is automatically retried.

A prototype alignment on the actual iPhone 17 / iOS 26.5 passed seven header controls: About, connection history, notifications, limited workers, active workers, Create and Settings. Each had a real native open and close assertion. This isolates the mechanism; the shipped adapter must still pass its own native regression, and candidate bundle version is checked separately.

Final native adapter proof on clean commit `6e48adb4c3f5`, build `2f5ec38aa23acc50`: `scripts/test-ios-session-recovery.mjs` passed four checks through the isolated server on port 18862. Native typing reached the visible fixture; input against its hidden tab returned 409 without dispatch; the same session survived 65 seconds without WebDriver commands; deleting that owned driver session followed by explicit Go produced a new id and `recovered_expired_session: true`. The fixture's command counter remained exactly one across refusal and recovery. Logs contain measured `hidden_native_tab`, `native_tab_aligned`, and `expired_session_released` signals. The fixture uses inherited 20px control fonts; an earlier default small input triggered Safari zoom and its new-tab check failed. This run does not certify pinch/auto-zoom behavior.

The final fixture also waits for Safari's new-tab discovery. These test-only adjustments leave the tested Rust adapter unchanged. Clean snapshot Clippy passed; the combined header browser matrix passed 12 cases. Fresh-origin native header evidence explicitly checks dashboard 0.9.931 and passes seven controls, with screenshots inspected. Full modal audit and production adoption evidence remain separately recorded on the board; they are not implied by these four fixture checks.

A subsequent four-modal audit passed scope-memory and connection-scope (including calibrated native scrolling out of the bottom, typing and dismissal), but Go stalled before board-focus, leaving worker-channel unmeasured. A staged navigation probe isolated an old Safari context returning 500 during its visibility read before the requested context was considered. Navigation now prioritizes matching URL metadata (ignoring fragment), then recent contexts for redirects. A regression makes unrelated contexts fail, so Go must reach its requested tab without touching them. `native_tab_aligned` now records `n_candidates` and `requested_url_match` beside the observed read count; no URLs are logged. The native test's cleanup also attempts explicit owner Stop when initial Go fails after persisting ownership.

Run the native recovery harness with `AMUX_IOS_TEST_URL=<isolated-server-url> AMUX_IOS_JOURNAL=<isolated-home>/browser-ios.json AMUX_SESSION=<test-worker> npm run test:ios:recovery`. The isolated server must have the native driver configured, and its previous owned test session must be stopped. The fixture sends no worker messages or board changes. Linux CI lacks the booted Xcode Simulator and local driver; this hardware requirement is explicitly recorded in the harness registry. Its unit tests run in ordinary Rust CI.

The full modal audit subsequently exposed tab accumulation: native deepLink opened a new tab for every Go. After three cases in that run, six test-origin Safari connections remained open and later tabs displayed blank/error pages despite the HTTP proxy answering 200. The failed run is retained in `scratch/ios-simulator-review/modal-native-complete-aligned/results.json`. Go now reuses an already-visible debugger tab through WebDriver URL navigation; only a hidden tab requires native alignment. Reuse emits `native_tab_reused` with measured population. A real HTTP regression repeats twelve navigations and refuses any deep-link dispatch, so this accumulation fails explicitly. Native repeat-navigation proof follows; this is not a complete-modal pass.
